<h2> <%=@email.object%> </h2>
<p> <%=@email.body%> </p>


<div class="row">
  <div class="col d-flex justify-content-center" >
    <%= form_tag(emails_path, method: "post", remote: true)  do %>
    <%= submit_tag("Supprimer", class: "btn btn-danger btn-lg") %>
    <%end%>

  </div>

  <div class="col">
    <%= form_tag(emails_path, method: "post", remote: true)  do %>
    <%= submit_tag("Marquer comme non lu", class: "btn btn-secondary btn-lg") %>
    <%end%>
  </div>

    <div class="col">

  </div>

</div>